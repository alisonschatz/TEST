info:
  name: lista-funcoes
  type: http
  seq: 1
  tags:
    - Função

http:
  method: GET
  url: "{{baseUrl}}/api/v1/funcoes?{{rt_query}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retorna lista paginada de funções da empresa. Filtros opcionais por funcao_id e alterado_data.
