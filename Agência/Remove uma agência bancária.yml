info:
  name: Remove uma agência bancária
  type: http
  seq: 4
  tags:
    - Agência

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/agencia"
  headers:
    - name: Authorization
      value: Bearer {{token_atual}}
  body:
    type: json
    data: |-
      {
        "agencia_id": 646476
      }

runtime:
  scripts:
    - type: before-request
      code: |-
        const perfil_escolhido = 2; 
        // ==============================================================
        // 1 = Perfil 1 - Unico Cliente
        // 2 = Perfil 2 - Unico ADM
        // 3 = Perfil 3 - Practice Cliente
        // 4 = Perfil 4 - Practice ADM
        // 5 = Perfil 5 - Parceiro Cliente Unico
        // 6 = Perfil 6 - Parceiro ADM Unico
        // 7 = Perfil 7 - Parceiro Cliente Practice
        // 8 = Perfil 8 - Parceiro ADM Practice
        // ===============================================================

        // Carrega os perfis
        let perfis = bru.getEnvVar("perfis");

        if (typeof perfis === "string") {
          perfis = JSON.parse(perfis);
        }

        // Pega o perfil
        let perfilDaVez = perfis[perfil_escolhido - 1];

        if (!perfilDaVez) {
          console.error(`Erro: O perfil número ${perfil_escolhido} não existe! Escolha de 1 a 8.`);
          return;
        }

        console.log(`>>> Executando requisição com: ${perfilDaVez.nome}`);

        // Requisição login
        const axios = require('axios');
        const credenciaisBase64 = btoa(`${perfilDaVez.user}:${perfilDaVez.pass}`);

        try {
          const resposta = await axios.post('https://api-auth-hml.sci.com.br/api/v1/auth/credencial/login', {}, {
            headers: {
              'Authorization': `Basic ${credenciaisBase64}`
            }
          });

          if (resposta.data && resposta.data.token) {
            // Atualiza a variável que o Header "Authorization" está lendo
            bru.setVar("token_atual", resposta.data.token);
          }
        } catch (error) {
          console.error("Erro ao autenticar perfil automaticamente:", error.message);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Agência bancária removida com sucesso
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": true,
            "status": 200,
            "mensagem": "Registro excluído com sucesso",
            "retorno": []
          }
  - name: 401 Response
    description: Erro de autenticação
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 403 Response
    description: Erro de permissão
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 403
      statusText: Forbidden
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": false,
            "status": 403,
            "mensagem": "Você não tem permissão para realizar esta ação.",
            "erros": {
              "dados.0.empresa_id": [
                "Usuário não está vinculado a essa empresa."
              ]
            }
          }
  - name: 409 Response
    description: "Agência em uso: exclusão bloqueada por dependências"
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 409
      statusText: Conflict
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": false,
            "status": 422,
            "mensagem": "The dados.0.descricao field must not be greater than 40 characters.",
            "erros": {
              "dados.0.descricao": [
                "The dados.0.descricao field must not be greater than 40 characters."
              ]
            },
            "retorno": null
          }
  - name: 422 Response
    description: Erro de validação
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": false,
            "status": 422,
            "mensagem": "The dados.0.descricao field must not be greater than 40 characters.",
            "erros": {
              "dados.0.descricao": [
                "The dados.0.descricao field must not be greater than 40 characters."
              ]
            },
            "retorno": null
          }
  - name: 500 Response
    description: Erro interno
    request:
      url: "{{baseUrl}}/api/v1/agencia"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "agencia_id": 15
          }
    response:
      status: 500
      statusText: Internal Server Error
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": false,
            "status": 422,
            "mensagem": "The dados.0.descricao field must not be greater than 40 characters.",
            "erros": {
              "dados.0.descricao": [
                "The dados.0.descricao field must not be greater than 40 characters."
              ]
            },
            "retorno": null
          }

docs: Remove uma agência bancária pelo agencia_id. A exclusão é bloqueada se a agência estiver em uso por outros registros no sistema. Disponível apenas para sistemas desktop e usuários do tipo Pessoa.
