info:
  name: Exclui serviço
  type: http
  seq: 8
  tags:
    - Autônomo

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/autonomo/servicos"
  body:
    type: json
    data: |-
      {
        "servico_autonomo_id": 178,
        "empresa_id": 900001
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Sucesso
    request:
      url: "{{baseUrl}}/api/v1/autonomo/servicos"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "servico_autonomo_id": 178,
            "empresa_id": 900001
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": true,
            "status": 200,
            "mensagem": "Registro apagado com sucesso",
            "retorno": []
          }
  - name: 401 Response
    description: Não autorizado
    request:
      url: "{{baseUrl}}/api/v1/autonomo/servicos"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "servico_autonomo_id": 178,
            "empresa_id": 900001
          }
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Erro de validação
    request:
      url: "{{baseUrl}}/api/v1/autonomo/servicos"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "servico_autonomo_id": 178,
            "empresa_id": 900001
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "sucesso": false,
            "status": 422,
            "mensagem": "The dados.0.descricao field must not be greater than 40 characters.",
            "erros": {
              "dados.0.descricao": [
                "The dados.0.descricao field must not be greater than 40 characters."
              ]
            },
            "retorno": null
          }

docs: Remove um serviço do sistema com base no seu ID. Esa ação é irreversível, a exclusão deve considerar se o serviço tem vínculos com autônomos.
